<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Pique" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if authenticated? %>
      <%= link_to "Collections", collections_path, class: "bg-pink-500 text-white px-4 py-2 rounded font-bold" %>
      <%= link_to "Flash Cards", flash_cards_path, class: "bg-pink-500 text-white px-4 py-2 rounded font-bold" %>
      <%= link_to "Quizzes", quizzes_path, class: "bg-pink-500 text-white px-4 py-2 rounded font-bold" %>
      <%= link_to "Categorization Quizzes", categorization_quizzes_path, class: "bg-pink-500 text-white px-4 py-2 rounded font-bold" %>

      <%= button_to "Sign Out", session_path, method: :delete, class: "bg-blue-500 text-white px-4 py-2 rounded font-bold" %>
    <% else %>
      <%= link_to "Sign Up", new_user_path, class: "bg-red-500 px-4 rounded text-white font-bold py-2" %>
      <%= link_to "Login", new_session_path, class: "bg-emerald-500 px-4 rounded text-white font-bold py-2" %>
    <% end %>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
